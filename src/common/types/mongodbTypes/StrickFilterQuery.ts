// import { FilterQuery } from 'mongoose';
//
// // Utility type to restrict properties to schema keys and allowed methods
// type StrictFilterQuery<T> = {
//   [P in keyof T]?: T[P] | { [K in AllowedMethods]?: T[P] };
// } & {
//   [K in AllowedMethods]?: T; // Allow methods at the root level if necessary
// };
